# MVC_Homework 專案改善建議

## 專案概述
此專案是一個使用 ASP.NET MVC 5 開發的記帳本應用程式，主要功能包括記錄收入和支出，並顯示帳目清單。

## 改善建議

### 1. 程式碼品質問題

#### 1.1 命名不當
**問題描述：**
- `AccountBook` 資料表欄位使用不規範的命名，例如：`Categoryyy`、`Amounttt`、`Dateee`、`Remarkkk`
- DbContext 類別名稱 `Model1` 缺乏語意
- Controller 命名空間錯誤：`MVC_Homework.Controllers.ViewModels` 應該是 `MVC_Homework.Controllers`

**建議改善：**
```csharp
// 修改前
public int Categoryyy { get; set; }
public int Amounttt { get; set; }
public DateTime Dateee { get; set; }
public string Remarkkk { get; set; }

// 修改後
public int Category { get; set; }
public int Amount { get; set; }
public DateTime Date { get; set; }
public string Remark { get; set; }
```

**影響範圍：**
- Models/EntityModel/AccountBook.cs
- Models/EntityModel/Model1.cs (建議改名為 AccountingDbContext.cs)
- Service/AccountingNoteService.cs
- Controllers/AccountingController.cs

#### 1.2 變數命名不清
**問題描述：**
- `AccountingNoteService.cs` 中的 `distin` 變數名稱毫無意義

**建議改善：**
```csharp
// 修改前
var distin = new AccountBook()

// 修改後
var accountBookEntity = new AccountBook()
```

### 2. 架構設計問題

#### 2.1 缺乏依賴注入 (Dependency Injection)
**問題描述：**
- Controller 直接 new Service 實例，違反依賴反轉原則
- DbContext 在 Service 中直接實例化，導致資源管理困難

**建議改善：**
```csharp
// 使用建構函式注入
public class AccountingController : Controller
{
    private readonly IAccountingNoteService _accountingNoteService;

    public AccountingController(IAccountingNoteService accountingNoteService)
    {
        _accountingNoteService = accountingNoteService;
    }
}
```

#### 2.2 缺乏 Repository 模式
**問題描述：**
- Service 層直接操作 DbContext，混合了業務邏輯和資料存取邏輯
- 不利於單元測試和維護

**建議改善：**
- 新增 Repository 層處理資料存取
- Service 層專注於業務邏輯
- 使用 Unit of Work 模式管理交易

#### 2.3 DbContext 生命週期管理不當
**問題描述：**
- `AccountingNoteService` 中的 `dbContext` 沒有正確釋放資源
- 可能導致記憶體洩漏和連線池耗盡

**建議改善：**
```csharp
public class AccountingNoteService : IDisposable
{
    private readonly Model1 dbContext;
    
    public void Dispose()
    {
        dbContext?.Dispose();
    }
}
```

### 3. 功能缺失

#### 3.1 缺乏錯誤處理
**問題描述：**
- Controller 的 `Add` 方法沒有處理異常情況
- 資料庫操作失敗時沒有適當的錯誤訊息給使用者

**建議改善：**
```csharp
[HttpPost]
[ValidateAntiForgeryToken]
public ActionResult Add(AccountingNoteViewModel source)
{
    try
    {
        if (ModelState.IsValid)
        {
            _accountingNoteService.Add(source);
            _accountingNoteService.Save();
            TempData["SuccessMessage"] = "新增成功！";
            return RedirectToAction("Add");
        }
    }
    catch (Exception ex)
    {
        ModelState.AddModelError("", "新增失敗：" + ex.Message);
    }
    return View(source);
}
```

#### 3.2 缺乏資料驗證
**問題描述：**
- `AccountingNoteViewModel` 的 `CostMoney` 沒有驗證是否為正數
- `CostDate` 沒有驗證日期範圍
- `Remark` 沒有最大長度限制

**建議改善：**
```csharp
public class AccountingNoteViewModel
{
    [Display(Name = "類別")]
    [Required(ErrorMessage = "請選擇類別")]
    public string CostType { get; set; }
    
    [Display(Name = "日期")]
    [Required(ErrorMessage = "請選擇日期")]
    [DataType(DataType.Date)]
    public DateTime CostDate { get; set; }
    
    [Display(Name = "金額")]
    [Required(ErrorMessage = "請輸入金額")]
    [Range(1, int.MaxValue, ErrorMessage = "金額必須大於 0")]
    public int CostMoney { get; set; }
    
    [Display(Name = "備註")]
    [StringLength(500, ErrorMessage = "備註最多 500 個字元")]
    public string Remark { get; set; }
}
```

#### 3.3 缺乏 CRUD 完整功能
**問題描述：**
- 只有新增和查詢功能
- 缺少編輯、刪除功能
- 無法查看單筆帳目詳細資訊

**建議改善：**
- 新增 Edit、Delete、Details 等 Action
- 提供更完整的帳目管理功能

#### 3.4 缺乏查詢和篩選功能
**問題描述：**
- `ShowAccounting` 只顯示所有資料，無法篩選
- 沒有分頁功能，資料量大時效能不佳
- 無法按日期範圍、類別、金額等條件查詢

**建議改善：**
- 實作分頁功能
- 加入日期範圍篩選
- 加入類別篩選
- 加入金額範圍篩選
- 加入排序功能

### 4. 安全性問題

#### 4.1 連線字串明碼儲存
**問題描述：**
- Web.config 中的連線字串直接明碼儲存
- 不符合安全最佳實踐

**建議改善：**
- 使用環境變數或加密方式儲存敏感資訊
- 在正式環境中使用 Azure Key Vault 或類似服務

#### 4.2 缺乏輸入驗證
**問題描述：**
- Controller 對使用者輸入的驗證不足
- 可能存在 SQL Injection 風險（雖然使用 Entity Framework 已有基本防護）

**建議改善：**
- 加強伺服器端驗證
- 確保所有使用者輸入都經過驗證和清理

### 5. 使用者體驗問題

#### 5.1 操作回饋不足
**問題描述：**
- 新增資料後沒有成功訊息
- 失敗時沒有明確的錯誤訊息
- 表單送出後頁面沒有重新整理或跳轉

**建議改善：**
- 使用 TempData 顯示成功/失敗訊息
- 成功後重新導向避免重複提交
- 使用 AJAX 提升使用者體驗

#### 5.2 UI 設計簡陋
**問題描述：**
- `ShowAccounting` 表格只顯示部分欄位（缺少備註）
- 沒有統計功能（總收入、總支出、餘額）
- 介面缺乏美化

**建議改善：**
- 顯示完整的帳目資訊
- 加入統計資訊顯示
- 美化表格樣式
- 加入圖表視覺化

### 6. 效能問題

#### 6.1 查詢效能
**問題描述：**
- `GetAll()` 一次載入所有資料到記憶體
- 沒有使用分頁，資料量大時效能低落

**建議改善：**
```csharp
// 使用分頁
public IEnumerable<AccountBook> GetPaged(int pageIndex, int pageSize)
{
    return dbContext.AccountBook
        .OrderByDescending(x => x.Date)
        .Skip((pageIndex - 1) * pageSize)
        .Take(pageSize)
        .ToList();
}
```

#### 6.2 資料庫索引
**問題描述：**
- AccountBook 表格沒有建立適當的索引
- 查詢效能可能受影響

**建議改善：**
- 在 `Date` 欄位建立索引
- 在 `Category` 欄位建立索引
- 考慮建立複合索引

### 7. 測試覆蓋率

#### 7.1 缺乏單元測試
**問題描述：**
- 專案中沒有任何單元測試
- 無法確保程式碼品質和正確性

**建議改善：**
- 為 Service 層撰寫單元測試
- 為 Controller 撰寫單元測試
- 使用 Mock 框架（如 Moq）模擬相依性
- 達到至少 80% 的程式碼覆蓋率

#### 7.2 缺乏整合測試
**問題描述：**
- 沒有整合測試驗證端到端流程

**建議改善：**
- 撰寫整合測試驗證完整的業務流程
- 測試資料庫互動

### 8. 文件不足

#### 8.1 缺乏 README 文件
**問題描述：**
- 專案沒有 README.md 說明文件
- 新開發者不易了解專案

**建議改善：**
- 撰寫詳細的 README.md
- 包含專案簡介、安裝步驟、使用說明等

#### 8.2 缺乏程式碼註解
**問題描述：**
- 程式碼缺乏適當的註解說明
- 複雜邏輯沒有解釋

**建議改善：**
- 為公開方法加入 XML 註解
- 為複雜邏輯加入說明註解

### 9. 版本管理問題

#### 9.1 Git 忽略檔案不完整
**問題描述：**
- 可能包含不必要的檔案在版本控制中

**建議改善：**
- 檢查並更新 .gitignore
- 排除 bin、obj、packages 等目錄
- 排除使用者特定檔案

### 10. 相依性管理

#### 10.1 套件版本過舊
**問題描述：**
- jQuery 1.10.2 版本過舊（2013年版本）
- Bootstrap 3.0.0 版本過舊
- Entity Framework 6.1.3 可以更新
- 許多套件有已知的安全漏洞

**建議改善：**
- 更新至較新且穩定的版本
- 使用 NuGet 套件管理工具檢查安全性漏洞
- 定期更新相依性套件

#### 10.2 不必要的相依性
**問題描述：**
- ApplicationInsights 相關套件可能未使用
- Identity 相關功能未完全實作

**建議改善：**
- 移除未使用的套件
- 或完整實作相關功能

## 改善優先順序

### 高優先級（必須改善）
1. 修正命名問題（欄位名稱、類別名稱、命名空間）
2. 加入錯誤處理機制
3. 實作依賴注入
4. 加強資料驗證
5. 修正 DbContext 生命週期管理

### 中優先級（建議改善）
1. 實作 Repository 模式
2. 加入 CRUD 完整功能
3. 實作分頁功能
4. 加入查詢和篩選功能
5. 改善使用者介面和體驗
6. 撰寫單元測試

### 低優先級（可選改善）
1. 更新相依性套件版本
2. 加入圖表視覺化
3. 撰寫整合測試
4. 完善文件
5. 效能優化（建立索引）

## 結論

此專案是一個基本功能完整的記帳本應用程式，但在程式碼品質、架構設計、安全性、使用者體驗等方面都有顯著的改善空間。建議優先處理高優先級的問題，以確保程式碼的可維護性和可擴充性，再逐步改善其他方面的問題。

透過這些改善，可以將此專案提升到符合業界標準的品質，並為未來的功能擴充打下良好的基礎。
